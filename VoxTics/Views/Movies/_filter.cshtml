@model VoxTics.Areas.Admin.ViewModels.MovieVMs.MovieIndexVM

<form method="get" asp-action="Index" class="card p-3 shadow-sm">
    <h5 class="mb-3">Filter Movies</h5>

    <div class="mb-3">
        @* <input type="text" name="search" value="@Model.Search" class="form-control" placeholder="Search movies..." /> *@
    </div>

    <div class="mb-3">
        <select name="categoryId" class="form-select">
            <option value="">All Categories</option>
            @foreach (var cat in Model.Categories)
            {
                @* <option value="@cat.Value" selected="@(Model.CategoryId?.ToString() == cat.Value ? "selected" : null)">
                    @cat.Text
                </option> *@
            }
        </select>
    </div>

    <button type="submit" class="btn btn-primary w-100">Apply Filter</button>
</form>
