@model VoxTics.Helpers.PaginatedList<VoxTics.Models.ViewModels.BookingVM>

<h2 class="text-2xl font-bold mb-6">My Bookings</h2>

<div id="bookingsContainer">
    @await Html.PartialAsync("_BookingList", Model)
</div>

<div class="mt-4">
    @await Html.PartialAsync("_Pagination", Model)
</div>
